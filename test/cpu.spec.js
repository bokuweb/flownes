import 'babel-polyfill';
// import { spy } from 'sinon';
import assert from 'power-assert';
import CPU from '../src/cpu/index';

describe ('cpu spec', () => {
  it('hoge', () => {
    assert.equal(1, 1);
  });
});
